# Roadmap

## 1) Faucet v1 polish
- Add waitlist join/verify gating for mDNDX
- Normalize faucet logging + error taxonomy
- Add per-wallet + per-IP throttling config
- Add faucet inventory telemetry
- Add admin-only dry-run + claim audit endpoint
- Improve UI copy + helpful error guidance

## 2) Universal Tx Queue / Nonce Rescue v2
- Replace/cancel UX for stuck userOps
- Add fee presets (slow/normal/fast)
- Expose userOp status polling + guidance
- Add receipt linkage for rescue actions
- Add “retry with higher fee” flow
- Add explorer deep links in UI

## 3) Wallet Health expansions (later)
- Contract tagging for common spenders
- Approvals history view (token allowances)
- Activity filters (incoming/outgoing/failed)
- Known-contract labels (DEX, bridge, etc.)
- Export report (CSV)
