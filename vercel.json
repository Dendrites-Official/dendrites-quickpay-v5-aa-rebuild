{
  "version": 2,
  "installCommand": "cd apps/dendrites-testnet-ui && NODE_ENV=development npm install",
  "buildCommand": "cd apps/dendrites-testnet-ui && npm run build",
  "outputDirectory": "apps/dendrites-testnet-ui/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
